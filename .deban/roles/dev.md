---
role: dev
owner: Gerald
status: active
last-updated: 2026-05-02
---

# Dev

## Scope
Implementation of the kakistogenesis.jsx artifact. Owns: React/JSX code, animation integration, the Names section markup, build/run workflow.

## Decisions
| Date | Decision | Rationale | Linked roles |
|---|---|---|---|
| 2026-05-02 | Add a Names section to the existing jsx listing six register-stratified candidate framings with their positioning text | Centralizes the naming work in the artifact itself rather than a separate doc; makes the register stack legible as a feature. Alternatives considered: separate names.md doc (rejected: orphaned from the diagram it names); footer-only one-liner credit (rejected: loses the register-stack idea, which is the actual product of the naming work). | [[pm]] [[ux]] |
| 2026-05-02 | Names content lives in a **separate tab** within the artifact, not inline with the diagram. Tab includes the six names with positioning text, plus map/territory and all-models-wrong meta-framing | Implements [[ux]] decision on tab placement and [[pm]] decision on meta-model framing. Implementation: requires the jsx to support a tab structure if it doesn't already (open question below). | [[ux]] [[pm]] |
| 2026-05-02 | **Blank-slate rebuild** as a static animated page with PWA capabilities. Existing kakistogenesis.jsx kept as PoC reference. Implementation dispatched to a Project Manager Agent with authority to make all executive calls and not stop until V1 is locally testable | User explicit: "do not stop until there is a V1 for me to test locally." Recommended stack (agent confirms or revises): Vite + React 18 + Tailwind + framer-motion + vite-plugin-pwa. Guided by mise-en-page (book-precision typography), frontend-design (distinctive aesthetic, avoid generic AI look), mobile-pwa (installable, offline). Mechanism content extracted from PoC; framing decisions inherited from [[pm]] and [[ux]]. | [[arch]] [[ux]] [[pm]] |
| 2026-05-02 | Animate the Filter Chamber: rainbow good-intent particles + dark-intent cloud both enter the maelstrom; rainbow particles progressively lose color and brightness; output is dark bad-outcome flow | Animation makes the asymmetry of the chamber legible as motion rather than as diagram-text. The particle-decay metaphor is load-bearing. Alternative considered: leave the diagram static (rejected: user explicitly wants animation). | [[ux]] [[arch]] |
| 2026-05-02 | **V1.1: Reimplement Filter Chamber as 2016-original rectangular layout.** Six concrete changes: (1) Add "2016 Original" tab displaying `public/origin-2016.jpeg` with provenance caption; (2) brighten rainbow input — full saturation, more density, more vibrant than V1's muted palette; (3) restore spatial mechanism mapping with colored dots and hover-tooltips showing each mechanism's brief; (4) particles random-walk through a subset of mechanism nodes losing saturation per visit (~0.85x); (5) replace single-vortex maelstrom with rectangular chamber containing the mechanisms; (6) output as dark stream branching into 2–3 estuary mouths. Keep: hero, typography, PWA wiring, mechanism data, "Other Names" tab. | [[ux]] [[pm]] |

## Dead Ends
| Date | What was tried | Why it failed / was rejected |
|---|---|---|
| 2026-05-02 | Modify the existing kakistogenesis.jsx in place to add animation and the Names tab | Rejected before implementation. User designated the file as proof-of-concept and authorized blank-slate rebuild. The existing 50KB single-component structure with hardcoded x/y coordinates does not extend cleanly to multi-tab + animated + PWA scope; a fresh project skeleton is faster than retrofitting. PoC file preserved as reference, not source-of-truth. |

## Lessons

## Open Questions
- [ ] Animation framework: framer-motion, react-spring, pure SVG SMIL, canvas, or GSAP — which fits the existing 50KB jsx without ballooning the bundle or fighting React's render cycle? — owner: Gerald — since: 2026-05-02
- [ ] How are the existing 11 mechanism nodes positioned (hardcoded x/y in the MECHANISMS array)? Does the animation overlay on those coords, or does it require a layout refactor? — owner: Gerald — since: 2026-05-02
- [ ] Does the artifact run as a standalone Claude/Code-style React Artifact (no build step, CDN imports), or in a Vite/Next project with Tailwind preconfigured? Constrains import surface for the animation library. — owner: Gerald — since: 2026-05-02
- [ ] Tab structure: does the current kakistogenesis.jsx have tab navigation already, or does the Names tab require introducing a tab system (with state, routing, persistence of selected tab on reload)? Affects scope: 30-line addition vs 200-line UI scaffolding. — owner: Gerald — since: 2026-05-02

## Assumptions
- [the existing jsx renders without a custom build pipeline beyond what an Artifact-style runner provides — Tailwind classes are honored, lucide-react imports resolve] — status: untested — since: 2026-05-02
- [particle count can stay low (dozens, not thousands) — the metaphor reads at low density and high count would invite physics-engine complexity that doesn't earn its keep] — status: untested — since: 2026-05-02

## Dependencies
Blocked by: animation framework choice (Open Question)
Feeds into: [[ux]] (visual aesthetic), [[pm]] (which name appears where in the UI)

## Session Log
2026-05-02 — V1.4 cost-field surgery SHIPPED. New `src/data/losses.ts` containing the ten loss objects verbatim from `what-is-lost.md`, `LossCategoryKey` type, `LOSS_CATEGORIES` map (material #9c7a4a / human #a87878 / epistemic #7a98a8 / social #7a9a7a), and computed `MECHANISM_TO_LOSSES` inverse map. All 19 unique `produced_by` IDs cleanly resolve against the 22 mechanism IDs — no orphans, no fixes needed. New `src/components/WhatIsLostSection.tsx` mirrors the catalogue's layout: § label, d-xl heading, intro paragraph verbatim, four-pill category filter (All ten / Material / Human / Epistemic / Social), 10 loss cards with left-rail `Produced by` mechanism tags (anchor-linked to `#m-<id>`), claim, OBSERVABLE caption, INCIDENCE italic mute, source citation + Reference external link, and verbatim italic closing paragraph. Section inserted in the chamber tab between MechanismsCatalogue and Footer. Surgery in `src/components/FilterChamber.tsx`: VIEW_H 880 → 1020; the 3-estuary system (DILUTED/DIMINISHED/HURTFUL) replaced by ten cost nodes laid out as two die-5 grids (Die A = Material & Human centered on `misallocation`, Die B = Social & Epistemic centered on `coordination-tax` displayed as "Trust"); flanking plumes (DILUTED warm-amber `#c9a070` left, HURTFUL dark-red `#8a1f1f` right) handle extreme-saturation outliers; thin turbulence-displaced filaments branch from `CHAMBER_EXIT` to each destination. Particle routing rewritten per option 2 from the brief: `finalSat > 0.6` → DILUTED, `finalSat < 0.04` → HURTFUL, otherwise weighted-by-overlap of `produced_by ∩ visited_mechanism_ids` (uniform fallback when totalW=0). Decay factor (0.6) and visit range (3–7) preserved. Cost nodes use the loss-category color, italic EB Garamond labels matching mechanism-node typography, ~44px touch targets. Hover surfaces a tooltip with `brief`; click opens a modal with full claim, OBSERVABLE, INCIDENCE, Produced-by anchor pills, reference, external link. Mechanism tooltip gained a `→ Produces:` footer (truncated at 2). Mechanism cards in `App.tsx` gained a `Produces:` line (after Reference, before field) listing each produced loss as an anchor link to `#l-<id>`; 19 of 22 mechanisms produce ≥1 loss (niskanen, conquest3, tullock-paradox produce none and correctly omit the line). `scrollMarginTop: 80` on both card types so anchor jumps clear the sticky tab bar. Mobile collapse: `useIsNarrow(768)` returns "narrow" mode that swaps each die from 5-pip layout to a 2x2 grid with the would-be-center pip tucked below — still visually paired, simpler. `tsc --noEmit` clean; `npm run build` succeeds (451.40 KB JS / 144.43 KB gzipped, PWA precache 9 entries). Decisions made: (a) the cost-node modal reuses the FilterChamber modal pattern (not LineageTab's) for visual coherence with the 22-mechanism click-throughs; (b) the dropdown footer line on mechanism tooltips is gated on `producedLosses.length > 0` so unreferenced mechanisms stay clean; (c) modal pill clicks call `closeModal` so the page anchor-scrolls into view; (d) flanks rendered as turbulence-displaced ink plumes (not estuary mouths) — same aesthetic register as before, recolored. Discipline preserved: chamber rectangle, particle physics, mechanism map, decay factor, visit range, hover/modal pattern, iOS-safe scroll-lock — all unchanged. Bottom caption "Diluted positive outcomes / to downright hurtful outcomes" + "2% of psychopaths ruining it" preserved verbatim, repositioned within the larger viewBox.

2026-05-02 — V1.3 mobile pass. Three new hooks under `src/hooks/`: `useBodyScrollLock` (iOS-safe scroll lock — `position: fixed` + restore scrollY, replacing the `overflow: hidden` on body that iOS Safari silently ignores; uses a CSS class `.is-scroll-locked` so the styling stays in CSS), `usePrefersReducedMotion`, `useIsNarrow` (matchMedia-based, addEventListener with addListener fallback for older Safari). FilterChamber: particle count `50 → 24` below 768px and `→ 12 static` under reduced-motion; `BadIntentCloud` count `8 → 4 → 0`; secondary rainbow swirl removed under reduced-motion; Estuary plume `motion.path` swapped for static `path` under reduced-motion; ParticleStream renders a single static `<circle>` at mid-path waypoint when reduced. Touch hit-area on each MechanismNode bumped from r=14 to r=22 (≈44px diameter). The double-fire `onTouchStart`+`onClick` race that was opening the modal before the tooltip could be read is fixed by removing the touchstart tooltip handler — touch users now get the modal directly, which contains the same brief plus the full claim and source. Modal close × buttons in both FilterChamber and LineageTab go from 32×32 to 44×44 (WCAG 2.5.5); `paddingRight` on metadata bumped from 44 to 56 to clear the larger button. Both modals use `useBodyScrollLock`. App.tsx TabBar: horizontal scroll on narrow viewports via new `.tabbar-scroll` class (no scrollbar, momentum on iOS, snap-proximity), mobile short labels (Filter/Lineage/Names/Origin/Works) via `sm:hidden` and `hidden sm:inline` Tailwind classes, button `min-height: 44`, `touchAction: manipulation` to suppress 300ms tap delay, `aria-current="page"` on active tab, `.tabbar-pad-top` reserves `env(safe-area-inset-top)` for notched iPhones. CategoryFilter pills bumped from `padding: 6px 12px` to `10px 14px` + `min-height: 44`. index.css: `.is-scroll-locked` class, `.tabbar-pad-top`, `.tabbar-scroll`, `@media (prefers-reduced-motion: reduce)` blanket rule (`animation-duration: 0.01ms !important` etc), `@supports (-webkit-touch-callout: none) input { font-size: 16px }` defensive iOS auto-zoom guard, `@media (max-width: 480px) .b-sm { font-size: 16px }`. `.d-hero` clamp lower bound dropped 3.4rem → 2.8rem to fit ペシ魔鉄則 at 320px. `tsc --noEmit` clean; `npm run build` succeeds (419.53KB / 136.26KB gz, PWA precache 9 entries). Bundle is unchanged in size — the perf wins are runtime, not bundle. Things deliberately NOT done: no PWA install button (passive installability is fine, intrusive UI would clash), no OG image (not asked, low priority), no `apple-touch-icon` PNG (svg favicon present and works on Safari 14+), `prefers-reduced-motion` is OS-level only — no in-app toggle. The dark cloud now disappears entirely on `prefers-reduced-motion: reduce`; this is the single biggest visual change for reduced-motion users — felt right because the cloud is purely motion expressing "bad input bypasses filtering," which is also a textual frame above the chamber.
2026-05-02 — V1.2 SHIPPED. Two new tabs: "Lineage" (II) and "Where it Works" (V). Tab order is now Chamber → Lineage → Other Names → Origin → Where it Works (Tab union extended in `src/App.tsx`). Lineage data file `src/data/lineage.ts` (19 entries across 8 traditions: Greek, Latin, Medieval Islamic, Chinese, Indian, Renaissance Italian, French, British) extends the mechanism schema with `original`, `tradition`, `prefigures`, `passage`, `honest_caveat`. Each card opens a modal (FilterChamber pattern reused) showing the verbatim passage on a tinted blockquote, the full claim, prefigures-as-anchor-pills (linking back to `#m-<id>` in catalogue), source citation, external Reference link, and a separately-labeled honest_caveat in italic mute. Two entries flagged `[verification pending]` (Ibn Khaldūn, Guicciardini) — passage placeholder, caveat explains the situation honestly rather than fabricating; UI surfaces a small oxblood "verification pending" badge on those cards. Where-it-Works tab uses three √-scaled bars (0.015% / 0.6–1.25% / ≈28% against 8B) with methodology notes per aperture; six case cards (Alpine commons, Spanish huertas, Subak Bali, Iriai Japan, Maine lobster + Alanya, Filipino zanjeras) using user-supplied populations verbatim; honest framing section preserves user's exact phrasings ("existence proof at toy scale", "communities willing to do the work", "shows nothing about whether it usually is, or whether the design transfers up"); a load-bearing 0.015% / 99.985% RatioReveal block with IntersectionObserver fade-in; closing Ostrom quote (Nobel 2009 press release wording, with link to the official Royal Swedish Academy press release URL). Build: `tsc --noEmit` clean; `npm run build` succeeds (418KB JS / 135.77KB gzipped, PWA precache 9 entries).
2026-05-02 — V1.1 SHIPPED. All six changes implemented and locally testable. (1) Third tab "Origin (2016)" added; renders `public/origin-2016.jpeg` at responsive max-width inside a paper-tone figure with an italic provenance caption and a section explaining how the original six plain-language mechanisms map to the modern 22-mechanism catalogue. (2) Rainbow input rebuilt: full-saturation palette (#ff2a3a / #ff7a1c / #ffd23a / #3acb4f / #2aa1ff / #7a4cff), 50 particles (up from 32), `mix-blend-mode: screen` retained, an SVG `<filter id="fc-bloom">` Gaussian-blur+merge added so the rainbow column visibly glows, plus a vertical `<linearGradient>` ink-in-water column behind the particles for the "rainbow ink" body the 2016 photo shows. (3) Spatial mechanism map: 22 nodes, deterministic seeded jitter inside a 3-column × 2-row category-panel grid, each node a colored dot + halo + italic EB Garamond label; hover/touch surfaces an HTML tooltip overlay (year · originator, name in Fraunces, brief in italic) with the category color as a left accent. (4) Particle random-walk: each particle picks 3–7 distinct nodes, sorts them roughly top-to-bottom, multiplies saturation by 0.85 per visit, animates cx/cy/fill/opacity through the waypoint chain via framer-motion `animate` arrays + `times`. Saturation interpolation is a hex→rgb mix with `#0a0a0c`. (5) Rectangular chamber: rounded-corner rect (rx=32) with a turbulence-displaced fill stroke + clean overlay border for legibility, 2x3 internal panels separated by dashed dividers, mono category headers tinted to category color, a left-margin italic "Mechanisms / That render / governments / inefficient" label echoing the 2016 page. (6) Output: dark convergence stream painted as three turb-displaced strokes converging from chamber-exit to three estuary outlets at viewport bottom — labeled DILUTED / DIMINISHED / HURTFUL — with darkness tints that vary per outlet (0.25 / 0.55 / 0.95) and pulse at different rates; particles route by final saturation (>0.6 → diluted, >0.4 → diminished, else hurtful). 2% psychopaths annotation preserved bottom-right. V1 chamber archived at `src/components/_archive/FilterChamber.v1.tsx`. Files: `src/components/FilterChamber.tsx` (rewritten, ~520 LOC, exports default + named), `src/components/OriginTab.tsx` (new), `src/App.tsx` (3-tab `Tab` union, third tab pane added, chamber section copy updated to reference random-walk + estuaries, "Origin (2016)" tab label III). Validation: `tsc --noEmit` clean, `npm run build` succeeds (358KB JS / 115KB gzipped, PWA precache 11 entries), dev server PID 36760 still alive at :5173, all assets 200. Decisions made vs brief: (a) used 50 particles, top of the 40–60 range, because the chamber rectangle is wider than the V1 vortex was tall — denser flow reads better; (b) tooltip implemented as an HTML overlay positioned via container-relative cursor coords rather than an SVG `<foreignObject>` because foreignObject hover focus is unreliable across browsers; (c) the dark cloud is rendered as 8 blurred dark puffs falling along scripted paths from the BAD_ENTRY column straight down past the chamber side and into the convergence stream — they don't visit nodes, they just visually carry the "bad input bypasses filtering" reading; (d) chose three estuaries (max of brief's 2–3) for finer outcome-gradient legibility. Known limitations: continuous-loop animation (no scroll-trigger / no reduced-motion respect — same as V1); dark cloud mostly visually decorative since it doesn't have the random-walk semantics; tooltip uses `clientX/Y` only (no SVG-coord projection) which is acceptable since the wrap is the offset parent.
2026-05-02 — V1.1 dispatched. Six changes: 2016-tab, brighter rainbow, spatial mechanism map with hover, particle random-walk, rectangular chamber + estuary output. PoC + V1 preserved. Dispatched to fresh PM agent (vault carries continuity).
2026-05-02 — V1 BUILT. PM agent shipped: Vite 7 + React 19 + TS + Tailwind 3 + framer-motion 11 + vite-plugin-pwa 1.2 + lucide-react. Dev server runs at http://localhost:5173/. Production build succeeds (~347KB JS gzipped 112KB, PWA precache 11 entries). Decisions made: (a) downgraded scaffolded Vite 8 → Vite 7 because vite-plugin-pwa 1.2 only supports Vite ≤7 (peer dep mismatch); same for @vitejs/plugin-react 6 → 4. (b) Kept React 19 (scaffold default). (c) Used vite-plugin-pwa generateSW mode (Workbox) with autoUpdate. (d) SVG-based animation chosen over canvas — lower particle count (~32) reads cleanly and SVG composes naturally with React via framer-motion. (e) Two-tab structure: "The Chamber" (hero + chamber + catalogue + footer) and "Other Names" (alternatives + meta-frame). Tab bar is sticky. (f) PoC enumerates 22 mechanisms across 6 categories (not 11/3 as the brief stated); preserved all 22 — PoC is source of truth per instructions. (g) Particle decay curve resolved as exponential-near-mouth (Math.pow(t, 1.6)) — see [[ux]] open question. (h) Hero typography uses Noto Serif JP for ペシ魔鉄則 (魔 in oxblood) at clamp(3.4rem, 11vw, 7.5rem). Filter Chamber animates with two counter-rotating dashed-circle layers + spiraling rainbow particles (screen blend mode) + black "shadow eaters" that ramp up opacity as particles approach center → visual desaturation. Bad-intent dark cloud particles spiral in from upper-right with blur. Output is uniform black plume, pulsing. Known V1 limitations: animation runs continuously (not scroll-triggered); SVG icons used for PWA (PNG generation deferred); no reduced-motion preference handling; bundle has framer-motion overhead (~70KB). Run with `npm run dev`.
2026-05-02 — Blank-slate V1 build dispatched to PM agent. Stack proposal: Vite + React + Tailwind + framer-motion + PWA plugin. PoC preserved.
2026-05-02 — Names section relocated to a separate tab (per [[ux]]/[[pm]]); tab-architecture scope question added.
2026-05-02 — Captured the animation brief and Names section addition; surfaced framework, layout, and runtime-target questions
